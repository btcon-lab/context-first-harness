# Seam QA 리포트 — {seam} / {YYYY-MM-DD}

> 정본: `docs/context/{seam}.md` · 소유: {소유 에이전트} · 검사자: seam-qa · 직전 리포트: {경로 또는 없음}
> **판정** V=구현 위반 · G=계약 공백 · S=정본 노후 · D=점진 이탈
> **심각도** MUST 위반=FAIL · SHOULD 이탈=WARN
> 정본이 진실이다. 구현을 근거로 계약을 정답 처리하지 않는다.

## 요약
| 소비자 | assertion | 정상 | V | G | S | D | SENSOR_ERROR | SKIP |
|--------|-----------|------|---|---|---|---|--------------|------|
| {consumer-a} | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| {consumer-b} | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

**전체 심각도:** {FAIL / WARN / PASS} — 가장 높은 것을 따른다
**위반 Rule ID:** {전부 나열. 하나만 적으면 나머지가 다음 회차에 새 문제처럼 다시 나온다}
**조치:** V {n}건(구현 수정) · G {n}건(계약 확정 후 반영) · S {n}건(정본 개정) · D {n}건(규칙 추가 검토)

> `SENSOR_ERROR`가 있으면 **이 리포트는 완결되지 않았다.** 미측정 항목을 통과로 읽지 말 것.

## 1. Assertion 전개
정본에서 도출한 검증 단문. `[TODO]`/`[OPEN]` 항목은 assertion이 아니라 G 후보로 아래 별도 기재.

| ID | Rule ID | 등급 | assertion | 정본 근거 |
|----|---------|------|-----------|----------|
| A1 | {SEAM}-P001 | MUST | {검증 가능한 단문} | `{seam}.md:{줄}` |
| A2 | {SEAM}-G001 | SHOULD | | |

**G 후보(계약 미확정 구간):** {정본의 [TODO]/[OPEN] 중 구현이 이미 진행된 항목}
**미분류 관측:** {어떤 Rule과도 매칭되지 않은 사실 — 규칙 추가가 필요하다는 신호}

## 2. 측정 결과 (사실만)
위반 여부를 판단하지 않고 관측된 사실만 적는다.

| assertion | 대상 | 관측된 사실 | 위치 | 상태 |
|-----------|------|------------|------|------|
| A1 | {소유자} | {실제로 어떻게 되어 있는가} | `{경로}:{줄}` | 측정됨 |
| A1 | {소비자} | | | 측정됨 |
| A2 | {소비자} | — | — | `SENSOR_ERROR`: {원인} |
| A3 | {소비자} | — | — | `SKIP`: {미구현 등} |

## 3. 판정
측정된 사실을 정본과 대조한 결과. 이 단계에서 코드를 새로 읽지 않는다.

### [{V/G/S/D}] [{FAIL/WARN}] {제목}
- **assertion / Rule ID:** A{n} / {SEAM}-P00{n} ({MUST/SHOULD})
- **정본:** `docs/context/{seam}.md:{줄}` — {계약 내용}
- **구현:** `{경로}:{줄}` ({소유자/소비자}) — {측정된 사실}
- **차이:** {무엇이 어떻게 다른가}
- **영향:** {이 차이로 발생하는 경계면 버그}
- **조치:** {대상 에이전트} — {구현 수정 / 계약 확정 후 반영 / 정본 개정 / 규칙 추가}
- **통지:** {G·S·D인 경우 소유 에이전트 + open-decisions 등재 여부}

## 4. 점진 이탈(D) 누적
개별 건은 위반이 아니지만 같은 방향으로 쌓이는 것. **한 회차로 판정하지 않는다.**

| ID | 감시 축 | 이번 회차 | 누적 | 임계 | 판정 |
|----|---------|----------|------|------|------|
| {SEAM}-D001 | {예: 계약 우회 직접 호출} | {n}건 | {n}건 | {n}건 | {미달 / **D**} |

임계 초과 시: 소유 에이전트에게 **명시적 규칙 추가**를 제안한다. 누적 근거(발생 위치 목록)를 함께 넘긴다.

## 5. 재검증 대상
| 대상 | 사유 |
|------|------|
| {(seam, 소비자)} | 수정 후 재실행 |
| {(seam, 소비자)} | `SENSOR_ERROR` 해소 후 **반드시** 재측정 |

계약이 바뀐 경우 이 seam의 **모든** 소비자를 재검사한다.
